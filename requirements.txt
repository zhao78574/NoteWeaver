# NoteWeaver 依赖 — 推荐用 pyproject.toml 安装:
#   pip install -e .
#
# 本文件保留供 CI / 开发环境使用，包含额外开发工具。
# 核心依赖以 pyproject.toml 为准。

# 核心依赖（与 pyproject.toml 同步）
openai>=1.0.0
faster-whisper>=1.0.0
Pillow>=9.0.0
torch>=2.0.0
PyYAML>=6.0
tqdm>=4.64.0
python-dotenv>=1.0.0
rich>=13.0.0
gradio>=4.0.0
rank_bm25>=0.2.0
sentence-transformers>=3.0.0

# 可选依赖
# keyring>=24.0.0           # OS 钥匙串存储（推荐替代 config.json 明文 key）
# chromadb>=0.4.0           # 向量检索
